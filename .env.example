# ArrNexus v3 does not require an .env file.
# Deploy the stack, browse to http://HOST:8484 and configure everything in the UI.
# Environment variables are accepted only as optional legacy/bootstrap fallbacks.
