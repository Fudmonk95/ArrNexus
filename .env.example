# ArrNexus v4 does not require an .env file.
# Deploy the Portainer/Docker Compose stack, browse to http://HOST:8484,
# create the administrator, then configure Connections/Settings in the UI.
# Environment variables remain optional legacy/bootstrap fallbacks only.
